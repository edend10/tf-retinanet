backbone:
  name: resnet
  details:
    type: resnet50
    weights: imagenet
    freeze: true

callbacks:
  snapshots_path: ../tutorial/snapshots_train
  project_name: tf_retinanet
  lr_scheduler: False
  optimizer: nadam
